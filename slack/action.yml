---
name: 'Slack'
description: 'Send action status to slack'

inputs:
  webhook-url:
    description: 'Slack incoming webhook URL'
    required: true
  jobs:
    description: 'Jobs'
    required: true
runs:
  using: 'node20'
  main: 'index.js'
